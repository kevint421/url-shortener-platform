apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: urlshortener
  labels:
    app: redis
    tier: infrastructure
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  selector:
    app: redis
